# 调试记录（DEBUG LOG）

本文档记录开发过程中踩过的坑、根因与最终解法，供后续排查参考。
所有问题均已闭环，下面的"当前状态"为线上运行版本的情况。

---

## 1. Zectrix 推送返回 `{"code":500,"msg":"服务器内部异常"}`
- **根因**：图片上传 multipart 字段名写成了 `file`，Zectrix Open API 要求是 **`images`**。
- **解法**：`eink.push_image` 里 `files={'images': (...)}`。
- **验证**：探针实测确认，返回 `code:0`。

## 2. 守护进程起不来 / 重启后不生效
- **根因**：QTS 的 BusyBox **没有 `nohup`、没有 `pgrep`**。旧脚本用 `nohup python3 main.py &` 在 SSH 会话结束即被杀；防重用 `pgrep -f "python3 main.py"` 匹配不到 `python3.12 -u main.py`。
- **解法**：用 `setsid "$PY" -u main.py < /dev/null &` 彻底脱离会话；防重改 `ps | grep '[m]ain.py'`（加方括号避免匹配到 grep 自身）。
- **位置**：`start-xueqiu.sh`。

## 3. 端口绑定失败（8080 被占用）
- **根因**：误把 D 盘 `config.json` 的 `server_port` 覆盖成 8080，但 QTS 系统占用了 8080。
- **解法**：统一改回 **8899**，并在 README/配置里标注"勿填 8080"。

## 4. 名字最上面一排缺行（字顶被裁）
- **根因**：旧 `rasterize` 基线 `int(cell_h*0.82)` 过高，高字符（字母 d/l/b、汉字"学"顶部横）被画布顶边裁掉 2px。
- **解法**：基线改为 `font.getmetrics()` 的 **ascent**，字顶落在 y=0，零裁切。

## 5. 名字最下面一排缺行（字底被裁）
- **根因**：字库格子高 `cell_h` 写死成 `size`（12/16/18），但宋体真实字形高 = `ascent+descent`，比 `size` 大 1~2px。基线=asc 不裁顶了，但底部 1px（"日/月"下缘、"y/p/g"勾）溢出被裁。
- **解法**：字库格高改为真实自然高度 `asc+desc`，`baseline=asc`，上下零裁切。同时修正字库 header 格式（宽高分字段存，避免 draw_char 越界）。

## 6. 名字带字母被截断成省略号（如 `PorcoRosso_dw`）
- **根因**：旧代码名字宽度用了 `min(uw, avail_w - 70)` 硬编码限制，拉丁字母名字字符多、总宽超阈值即被砍。
- **解法**：去掉硬编码宽度限制，名字行完整显示（仅内容行超宽才截断加省略号）。

## 7. 时间格式 / 字号不统一
- **根因**：标题栏用 `08-09` 格式、每行用另一种格式；标题时间与每行时间字号不同。
- **解法**：统一 `_fmt_time` 输出「M月D日 H:MM」（无前导零），标题栏与每行时间戳都用 12px 同字号。

## 8. 黑体在 1-bit 墨水屏显示异常
- **根因**：早期用 Noto Sans SC / 雅黑，细笔画在二值化（THRESHOLD）下被吃掉（如 o→c），且高对比下显得"不正常"。
- **解法**：最终改用**宋体**（`simsun.ttc`），细笔画衬线在 1-bit 下清晰细腻。曾尝试 Hiragino Sans GB W3 等细黑体，用户反馈"不好看"暂未采用（字库保留备用）。

## 9. 灰阶标题栏需自绘
- **根因**：Zectrix 推送 `dither=false`，设备端不负责抖动；想要"灰底"必须在画布里自己点阵化。
- **解法**：`Canvas.fill_gray()` 用 Bayer 4×4 有序抖动模拟 1-bit 灰阶，`density`=黑点占比。
- **当前状态**：定稿用纯黑底（`title_gray=0.0`），未启用灰阶。

## 10. 推到设备后一段时间被别的卡片刷掉
- **根因**：Zectrix「墨卡」页有多张卡片，自定义传图只是其中一张，会被"心灵鸡汤"等模板卡轮播覆盖。
- **解法（设备端，需用户手动）**：小程序墨卡里删掉"心灵鸡汤"等模板卡，设默认主页/待机=墨卡。
- **代码侧**：推送走 `pageId=1` 持久化槽位，设备端设置正确即稳定显示。

## 11. 设备刷新频率 vs 推送频率 mismatch
- **现象**：NAS 每 10 分钟推一次新图，但 Zectrix 设备端被设成 1 分钟刷新一次。
- **影响**：设备连续 10 次刷同一张旧图才等到新图，白白耗电 + 加速屏老化。
- **建议**：设备端刷新间隔也设为 ~10 分钟，或开启"仅推送唤醒"模式（若固件支持）。

## 12. NAS live 目录是 `run/` 不是父目录
- **根因**：部署时父目录 `/share/nas02/xueqiu-watch-v2` 只是源码副本，真正运行进程 cwd 是 `.../run`，运行态文件（`xw.log`、`state.json`、`cookies.json.enc`）都在 `run/`。
- **解法**：`deploy_push.py` 同时同步到 `run/` 和父目录两处，并明确"live 目录是 run/"。老 `deploy-to-nas.py` 只推父目录会推错地方，已弃用。
